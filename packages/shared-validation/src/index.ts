// Zod validation schemas, added incrementally as each screen/module is migrated
// to mobile (see the migration master prompt's one-module-at-a-time workflow).
// Intentionally empty for now — neither the web app nor the backend currently
// validate with Zod, so there is nothing to port yet.
export {};
